name: one-proxy-panel-base-image

on:
  push:
    branches: [main]
    paths:
      - 'apps/one-panel-api/go.mod'
      - 'apps/one-panel-api/go.sum'
      - 'apps/one-proxy-panel/package.json'
      - 'apps/one-proxy-panel/package-lock.json'
      - 'docker/one-proxy-panel-base.Dockerfile'
      - '.github/workflows/one-proxy-panel-base-image.yml'
  pull_request:
    paths:
      - 'apps/one-panel-api/go.mod'
      - 'apps/one-panel-api/go.sum'
      - 'apps/one-proxy-panel/package.json'
      - 'apps/one-proxy-panel/package-lock.json'
      - 'docker/one-proxy-panel-base.Dockerfile'
      - '.github/workflows/one-proxy-panel-base-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build panel base image
        run: docker build -f docker/one-proxy-panel-base.Dockerfile -t ghcr.io/${{ env.OWNER_LC }}/oneproxy-panel-base:latest .

      - name: Push panel base image
        if: github.event_name != 'pull_request'
        run: docker push ghcr.io/${{ env.OWNER_LC }}/oneproxy-panel-base:latest
