name: chrome-extension-ci

on:
  push:
    branches: [main]
    paths:
      - 'apps/extension/chrome/**'
      - 'apps/extension/chrome/tools/**'
      - 'apps/extension/chrome/test/**'
      - '.github/workflows/chrome-extension-ci.yml'
  pull_request:
    paths:
      - 'apps/extension/chrome/**'
      - 'apps/extension/chrome/tools/**'
      - 'apps/extension/chrome/test/**'
      - '.github/workflows/chrome-extension-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.node-version'

      - name: Build extension service worker
        run: node apps/extension/chrome/tools/build_background_bundle.mjs

      - name: Static extension checks
        run: |
          node apps/extension/chrome/tools/validate_extension.mjs
          python3 apps/extension/chrome/test/assert_no_sensitive_ak_in_smoke.py

      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.60.0
          npx playwright install --with-deps chromium

      - name: Service worker smoke test
        run: xvfb-run --auto-servernum node apps/extension/chrome/test/service_worker_smoke.mjs
