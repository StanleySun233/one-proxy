name: one-proxy-node-image

on:
  push:
    branches: [main]
    paths:
      - 'apps/node/api/**'
      - 'apps/node/web/**'
      - 'docker/one-proxy-node.Dockerfile'
      - '.github/workflows/one-proxy-node-image.yml'
      - '!apps/node/api/go.mod'
      - '!apps/node/api/go.sum'
  pull_request:
    paths:
      - 'apps/node/api/**'
      - 'apps/node/web/**'
      - 'docker/one-proxy-node.Dockerfile'
      - '.github/workflows/one-proxy-node-image.yml'
      - '!apps/node/api/go.mod'
      - '!apps/node/api/go.sum'
  workflow_run:
    workflows: ['one-proxy-node-base-image']
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect proxy-node base changes
        id: base
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_run" || "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "skip_final=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch origin "${{ github.base_ref }}" --depth=1
            RANGE="origin/${{ github.base_ref }}...HEAD"
          elif [[ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            RANGE="${{ github.event.before }}...${{ github.sha }}"
          else
            RANGE="HEAD~1...HEAD"
          fi
          changed="$(git diff --name-only "$RANGE" || true)"
          if printf '%s\n' "$changed" | grep -Eq '^(docker/one-proxy-node-base\.Dockerfile|apps/node/api/go\.(mod|sum))$'; then
            echo "skip_final=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip_final=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Lowercase owner
        if: steps.base.outputs.skip_final != 'true'
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      - name: Login to GHCR
        if: steps.base.outputs.skip_final != 'true' && github.event_name != 'pull_request'
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Pull proxy-node base image
        if: steps.base.outputs.skip_final != 'true'
        run: docker pull ghcr.io/${{ env.OWNER_LC }}/oneproxy-node-base:latest

      - name: Build proxy-node image
        if: steps.base.outputs.skip_final != 'true'
        run: docker build -f docker/one-proxy-node.Dockerfile --build-arg NODE_BASE_IMAGE=ghcr.io/${{ env.OWNER_LC }}/oneproxy-node-base:latest -t ghcr.io/${{ env.OWNER_LC }}/oneproxy-node:latest .

      - name: Push proxy-node image
        if: steps.base.outputs.skip_final != 'true' && github.event_name != 'pull_request'
        run: docker push ghcr.io/${{ env.OWNER_LC }}/oneproxy-node:latest
