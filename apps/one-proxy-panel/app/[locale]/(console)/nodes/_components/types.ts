'use client';

export type BootstrapFormValues = {
  targetId: string;
  nodeName: string;
  nodeMode: string;
  scopeKey: string;
  parentNodeId: string;
  parentReachableUrl: string;
  publicHost: string;
  publicPort: string;
};
